'use strict';
/* exported getFirstChar */
// PSEUDOCODE:
// Define a function named 'getFirstChar', that has one parameter, 'string'.
// inside the code block return the first character of 'string' using brackets
function getFirstChar(string) {
  return string[0];
}
