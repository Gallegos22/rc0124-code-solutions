'use strict';
/* exported getLastChar */
// Define a function named 'getLastChar', that has a parameter of 'string'
// return the last character of any string passed in as an argument using the length property
function getLastChar(string) {
  return string[string.length - 1];
}
