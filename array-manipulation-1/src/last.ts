/* exported last */
// Define a function named last with a parameter of array
// return the last element of an array using the .length property

function last(array: []): unknown {
  return array[array.length - 1];
}
